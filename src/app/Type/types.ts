export type ItemPix = {
    id: string,
    name: string,
    bank: string,
    selected: boolean
}

export type userCrete = {
    name: string,
    email: string,
    password: string,
    confirmPassword: string,
    termChecked: boolean
}

export type userLoged = {
    id: string,
    name: string,
    email: string
}