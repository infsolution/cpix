import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  viewClose: {
    position: "absolute",
    top: 0,
    right: 0,
    bottom: 0,
    backgroundColor: "black",
    zIndex: 1,
  },
});
