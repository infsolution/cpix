import {
  ResetPasswordForm,
  ResetPasswordFormProps,
} from "@/components/ResetPasswordForm";

export const FormChangePasswordModals = ({ close }: ResetPasswordFormProps) => {
  return <ResetPasswordForm close={close} />;
};
